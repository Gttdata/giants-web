import { DepartmentworkingDetails } from './departmentworking-details';

describe('DepartmentworkingDetails', () => {
  it('should create an instance', () => {
    expect(new DepartmentworkingDetails()).toBeTruthy();
  });
});
