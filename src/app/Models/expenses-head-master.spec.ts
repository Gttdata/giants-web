import { ExpensesHeadMaster } from './expenses-head-master';

describe('ExpensesHeadMaster', () => {
  it('should create an instance', () => {
    expect(new ExpensesHeadMaster()).toBeTruthy();
  });
});
